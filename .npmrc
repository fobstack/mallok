# Allow the two packages that legitimately need postinstall scripts.
# Everything else is installed without running lifecycle scripts.
only-built-dependencies[]=esbuild
only-built-dependencies[]=workerd
