<!doctype html>
<html lang="{{ page.locale }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% if page.kind == "home" %}{{ site.name }} — {{ site.tagline }}{% else %}{{ page.title }} | {{ site.name }}{% endif %}</title>
  {% if page.description != "" %}<meta name="description" content="{{ page.description }}">{% endif %}
  <link rel="canonical" href="{{ page.canonical }}">
  {{ page.head }}
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Source+Serif+4:opsz,wght@8..60,400;8..60,600;8..60,700&family=Archivo:wght@400;500;600;700&display=swap">
  <link rel="stylesheet" href="{{ theme.asset_base }}/style.css">
</head>
<body>
  <a class="skip" href="#main">{{ t.skip }}</a>
  {% render "partials/header" %}
  <main id="main">{% block content %}{% endblock %}</main>
  {% render "partials/footer" %}
</body>
</html>
